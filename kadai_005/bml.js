// 定数に体重を代入
const weight = 68;
// コンソールへの出力
console.log(weight);

// 定数に身長を代入
const height =1.7;
// コンソールへの出力
console.log(height);

// 定数にbmiを代入
const bmi = (weight / (height * height));
// コンソールへの出力
console.log(bmi);